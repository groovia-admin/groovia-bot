// The dashboard uses the collection endpoint for listing and creating shops.
// The handler implementation is shared until the item route gains its own
// PATCH and DELETE handlers.
export { GET, POST } from './[id]/route'
